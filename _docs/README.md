Starting every new chat:
Upload all three files and open with something like: "I'm continuing work on engagingcourses.com. Here are my context files — please read them before we start." That's all Claude needs to be fully up to speed.
Ending every session:
Ask Claude to generate a new changelog entry — something like "Please write a changelog entry for EC-CHANGELOG.md summarizing what we did today." Paste it at the top of the changelog file.
What each file does:

EC-CONTEXT.md — Everything permanent. Tech stack, design system, file structure, key decisions. Rarely changes.
EC-CHANGELOG.md — Running log of sessions. You maintain this by pasting new entries at the top.
EC-BLOG-INDEX.md — Every blog post ever written, with details. Update when posts are added or edited.
